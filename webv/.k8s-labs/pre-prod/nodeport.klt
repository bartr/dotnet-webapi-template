apiVersion: v1
kind: Service
spec:
  type: NodePort
  selector:
    app: {{klt.AppName}}
  ports:
    - port: {{klt.Port}}
      targetPort: {{klt.Port}}
      nodePort: {{klt.NodePort}}

metadata:
  namespace: {{klt.Namespace}}
  labels:
    app: {{klt.AppName}}
  name: {{klt.AppName}}
