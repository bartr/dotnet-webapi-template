commands:
- name: cluster
  short: Local cluster commands
  scriptCommands:
    - name: create
      short: Create a local cluster
      path: cluster/create

    - name: delete
      short: Delete the local cluster
      path: cluster/delete
