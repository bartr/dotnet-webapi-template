commands:
- name: cluster
  short: Local cluster commands
  scriptCommands:
    ## todo - remove create
    - name: create
      short: Create a local cluster
      path: cluster/create

    - name: flux-install
      short: Install Flux on the local cluster
      path: cluster/flux
