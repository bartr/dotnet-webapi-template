###
# check commands
###

commands:
- name: check
  short: Check local apps
  commands:
    - name: heartbeat
      hidden: true
  scriptCommands:
    - name: csapp
      short: Check CSApp
      path: check/app

    - name: webv
      short: Check WebValidate
      path: check/webv

    - name: prometheus
      short: Check Prometheus service
      path: check/prometheus

    - name: grafana
      short: Check Grafana service
      path: check/grafana

    - name: flux
      short: Check Flux service
      path: check/flux
